$kind: http-request
description: Saves document uploads into documents table for the active filing.
url: "{{baseUrl}}/clients/{{clientId}}/onboarding/save"
method: POST
headers:
  - key: ""
    value: ""
  - key: ""
    value: ""
body:
  type: json
  content: |-
    {
      \
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 8000
