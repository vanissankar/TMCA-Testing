$kind: http-request
description: Creates client_details and initializes filing. Stores
  data.client_id into collection variable clientId.
url: "{{baseUrl}}/clients/onboarding/save"
method: POST
headers:
  - key: ""
    value: ""
  - key: ""
    value: ""
body:
  type: json
  content: |-
    {
      \
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
scripts:
  - type: afterResponse
    code: >-
      const json = pm.response.json();

      if (json?.data?.client_id) { pm.collectionVariables.set("clientId",
      json.data.client_id); }
    language: text/javascript
order: 7000
