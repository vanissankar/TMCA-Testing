$kind: http-request
description: Saves loan rows into loans table.
url: "{{baseUrl}}/clients/{{clientId}}/onboarding/save"
method: POST
headers:
  - key: ""
    value: ""
  - key: ""
    value: ""
body:
  type: json
  content: |-
    {
      \
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 12000
