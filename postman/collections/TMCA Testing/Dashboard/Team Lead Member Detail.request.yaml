$kind: http-request
description: Team lead dashboard member detail.
url: "{{baseUrl}}/dashboard/team-lead/member/{{memberId}}?page=1&limit=10&search="
method: GET
queryParams:
  page: "1"
  limit: "10"
  search: ""
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 6000
