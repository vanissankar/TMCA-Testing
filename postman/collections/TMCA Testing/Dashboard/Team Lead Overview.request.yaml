$kind: http-request
description: Team lead dashboard overview.
url: "{{baseUrl}}/dashboard/team-lead/overview?page=1&limit=10&search="
method: GET
queryParams:
  page: "1"
  limit: "10"
  search: ""
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 5000
