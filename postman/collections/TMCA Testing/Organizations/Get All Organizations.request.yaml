$kind: http-request
description: Returns all organizations for admin; other roles get their own organization scope.
url: "{{baseUrl}}/organizations"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 3000
